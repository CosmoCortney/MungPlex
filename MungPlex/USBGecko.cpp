#include <iostream>
#include "USBGecko.hpp"

bool MungPlex::USBGecko::Init()
{
   /* if ((_ftdi = ftdi_new()) == 0)
    {
        std::cerr << "Failed to initialize FTDI context\n";
        return false;
    }*/
	return true;
}
